# Playwright 用例需求模板

请基于当前仓库编写或补充 Playwright 自动化用例，并严格遵守仓库中的 `AGENTS.md`、现有目录结构、命名规范、分层约束与代码风格。

## 任务目标

为以下业务场景实现一个可运行的 Playwright Test 用例，并确保其适合作为：

- `【smoke / e2e / regression】`

## 执行前要求

在开始编码前，请先完成以下事项：

1. 阅读并遵守 `AGENTS.md`
2. 优先复用当前仓库已有的：
   - pages
   - flows
   - fixtures
   - helpers / utils
   - 测试数据组织方式
3. 检查仓库中是否已经存在同目标或高相似用例
4. 若已有同目标或高相似用例，优先在原用例上增量修改，不重复创建
5. 如现有能力不足，可在符合分层原则前提下补充代码
6. 不要破坏现有风格，不要引入与仓库规范冲突的实现方式

## 场景说明

- 场景名称：`【填写场景名】`
- 用例类型：`【smoke / e2e / regression】`
- 业务目标：`【填写最终要验证的业务结果】`

## 入口与前置条件

1. 必须从：`【首页 URL】` 进入
2. 若出现 `license` 选择页：`【填写处理方式】`
3. 登录方式：`【员工口令 / 账号密码 / 免登录等】`
4. 其他前置条件：`【如桌台、区域、权限、设备状态等】`

## 测试数据

- `【数据项1】`：`【值】`
- `【数据项2】`：`【值】`
- `【数据项3】`：`【值】`

## 业务步骤

1. `【步骤1】`
2. `【步骤2】`
3. `【步骤3】`
4. `【步骤4】`

## 断言要求

至少覆盖以下断言：

1. `【断言1】`
2. `【断言2】`
3. `【断言3】`
4. `【断言4】`

如过程中产生关键中间结果，请先在当前页完成必要校验，再进入后续页面做最终比对。

如涉及后续查询、历史列表、详情对比或记录定位，请补充：

- `【用于后续精确定位记录的关键字段】`
- `【允许使用的定位方式】`
- `【若只能按最新记录匹配时的原因与风险说明】`

如涉及动态数据、称重数据、开放价格或后续页展示能力不完整的数据，请补充：

- `【优先校验的稳定字段】`
- `【允许降级校验的字段】`
- `【降级校验成立的前提】`
- `【最终输出中需要说明的限制】`

## 实现约束

- 使用 `Playwright Test`
- `describe` / `test` / `test.step` 使用中文命名
- 页面动作放在 `pages`
- 业务编排放在 `flows`
- 测试文件只保留测试意图与断言，不堆砌底层操作细节
- 优先使用 `data-testid`
- 禁止使用 `waitForTimeout`
- 禁止通过直跳业务内页 URL 绕过正常入口流程
- 优先复用已有 page object；如需补充，请保持 pages / flows 职责清晰
- 选择器要稳定，避免过度依赖文案、层级、nth()
- 如定位不稳定，应优先补充 page object / flow，而不是把复杂 locator 直接堆在 spec 中
- 如业务数据为动态数据，请在测试中保存关键数据，并在后续页面进行对比校验

## 代码产出要求

请直接修改或新增以下内容（按实际需要）：

- `pages/...`
- `flows/...`
- `tests/...`

并确保：

1. 新增代码可运行
2. 命名符合仓库风格
3. 不引入无关重构
4. 仅实现本次场景所需的最小必要改动

## 元数据

- `tag`: `【如 @smoke】`
- `annotation`:
  - `【Jira / issue 链接】`

## 输出要求

完成后请输出：

1. 本次新增或修改了哪些文件
2. 核心实现说明
3. 执行命令
4. 实际验证结果
5. 如未能完成，请明确说明卡点与原因，不要伪造完成状态

补充要求：

- 必须运行相关 Playwright 用例，并基于真实执行结果汇报
- 不允许仅根据代码推断“理论上应当通过”
- 如果只完成了代码修改但未完成验证，必须明确说明未验证部分与原因
