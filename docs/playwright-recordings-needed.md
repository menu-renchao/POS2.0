# Playwright 待补充录制清单

本文档记录当前自动化中缺少真实页面 DOM 契约、需要人工录制补充的场景。

订单操作回归当前共有 **6 条 skipped**：其中报表 Fee/Unpaid 相关 **5 条已确认暂缓开发**；POS-32954 因“POS NG 不适用”保留 skip 但不需要录制。点单页面提示词当前剩余 **1 条**：`ORDER-PAGE-045` 为报表相关用例，已排除出当前开发批次。

## 提交格式

1. 每个场景单独录制为一个 `test('test', async ({ page }) => { ... })`。
2. 请把脚本放在三反引号 `ts` 代码块中，避免 `_`、`*` 等字符被 Markdown 转义。
3. 保留录制生成的原始 `data-testid`，动态订单 ID 可以保留，自动化落地时会替换为运行期订单号。
4. 不需要补录登录、License 选择等已有公共流程，除非问题就发生在这些步骤。
5. 若页面显示的业务结果与预期不同，请同时附一张最终页面截图并注明实际结果。

## 暂缓开发：报表相关用例

### 1. POS-31081 / POS-30566 / POS-32004 / POS-32016 / POS-32023：报表首页 Fee 与 Unpaid

> 已确认暂缓开发。当前不实现、不采集页面契约，也不再请求录制；恢复开发时再重新核对报表入口与数据权限。

- 恢复开发时参考：`readReportFeeAmount()` 和 `readReportHomeUnpaidAmount()` 仍为空实现；Report 入口会再次要求员工口令，报表加载在 `#thirdApp` 下的外部云报表 iframe。
- 恢复开发前需重新确认外部报表数据权限、Fee/Unpaid 的真实 DOM 契约，以及日期、员工、班次或门店筛选范围。

## 已完成自动化但当前环境直接失败

以下用例已经完成 Page、Flow、正式测试和清理接口，不再需要录制；正式用例不使用 `skip` 或 `test.fail(...)`，当前会直接报告 Failed。

### ORDER-PAGE-024：提示词 40 POS-34106 点单页面，礼品卡实体卡新增页面，手机号输入框，手机号输入正确，可新建卡成功（已自动化）

- Jira：`POS-34106`。
- 无需继续录制：2026-07-20 已从真实 UI 确认入口不在 Payment，而是 POS 首页稳定功能卡 `pos-ui-function-card-gift_card`。点击后在宿主页打开 Gift Card 管理页，`Add New Card` 进入实体卡表单；Physical Card 的真实字段为 `#vpCustName`、`#vpCardNum`、`#vpPhoneNum`，保存控件为 `#vpCardeditbt`，手机号输入 `2544296158` 后页面会格式化为 `(254)429-6158`。
- 当前环境阻塞：正式用例点击 Save 后正确发出 `POST /kpos/ws/kposService`，SOAP 动作为 `SaveGiftCardType`，请求包含 number、issuedTo、cellphone、`cardType=PHYISCAL`，但环境固定返回 HTTP 404，页面提示 `Network error, please check whether server working or network connect correctly!`；用例因此直接 Failed，不使用 skip 或预期失败。
- 已落地：`pages/gift-card.page.ts` 负责唯一真实 DOM 契约、表单回显和保存请求读取，`flows/gift-card.flow.ts` 负责编排创建，`tests/py-migrate/gift-card.spec.ts` 断言手机号格式化与请求内容；`api/clients/gift-card-api.client.ts` 在服务恢复且创建成功后按卡号执行 finally 清理。

### 订单操作剩余批次：18 条已写入正式测试

`tests/py-migrate/order-operation-remaining.spec.ts` 当前可枚举 **18 条**，均未使用 `skip`、`fixme` 或 `test.fail(...)`。

- 已真实通过：POS-35134、POS-35135、POS-35150、POS-35163、POS-35166、POS-35307、POS-37798。
- Price 折扣授权成功后弹窗仍不关闭：POS-35142、POS-36140、POS-36148、POS-36156、POS-42085、POS-42086。
- 其他普通直接失败：POS-35155（退款菜再次送厨仍产生 `sentTime`）、POS-35173（信用卡模拟器返回 HTTP 400）、POS-35498（真实单菜加收 Confirm 后 Charge 弹窗不关闭）。
- 套餐流程已完成整改：POS-35307 已通过；POS-42631 已按原需求实现“先送厨套餐、Recall 追加子菜与 option、保存、再次 Recall 打单”的完整路径。保存请求已确认包含新增子菜和 `option1`，打单接口返回 HTTP 200；当前产品返回的订单状态为 `SUBMITTED` 而非要求的 `PRINTED`，因此正式用例直接 Failed，不使用 skip 或预期失败。
- 2026-07-21 覆盖审计：订单操作源文件 92 个 Jira key 均已出现在正式测试中；点单页面源文件 65 个 Jira key 仅缺 POS-22657，该条为已暂缓的报表用例。该结果只表示可追溯覆盖，不代表上述直接失败用例已经通过。

## 点单页面提示词待补充

以下 1 条需求与点单页面提示词覆盖矩阵严格一一对应。该条已经确认暂缓开发，不需要当前录制；录制编号保持原顺序和映射，不重编号。

### ORDER-PAGE-045：提示词 63 自定义类型-报表显示Report-Overview-预览显示自定义类型的报表数据-数据正确

- Jira：`POS-22657`。
- 开发状态：报表相关用例已确认暂缓开发；当前不实现、不采集页面契约，也不再请求录制。
- 恢复开发时参考：需覆盖自定义 Delivery 订单保存、Report Overview 类型筛选及数量/金额增量，并恢复 Cloud Report 配置；实施前重新确认外部报表访问和数据权限。

## 已完成自动化但当前产品直接失败

以下两条已找到真实 `Price` 折扣入口并完成 Page、Flow、正式用例及保存/Recall 断言，不再需要录制。为保留定位依据，历史录制说明暂存于下方；两条正式用例均不使用 `skip` 或 `test.fail(...)`，当前会直接报告 Failed。

### ORDER-PAGE-049：提示词 7 POS-42886 菜品操作改价时可以选择单菜折扣（已自动化）

- Jira：`POS-42886`。
- 当前状态：`tests/py-migrate/order-page-regression.spec.ts` 已创建隔离 10% 折扣并使用真实 `Price` 弹窗中的 `pos-ui-option-{discountId}`；授权接口返回 HTTP 200、`code=0` 后，`Price` 弹窗仍不关闭和提交，因此正式用例直接 Failed。
- 已知前置：可从 POS 首页进入点单页、折前 Subtotal 为 8.80 的普通菜，以及产品真实支持的单菜 Discount 入口；不得把 `Custom Charge` 当作 Discount。
- 请从 POS 首页开始录制：
  1. 最小录制路径：从 POS 首页建立员工上下文，进入堂食无桌点单并添加目标普通菜，读取并保留折前 Subtotal 8.80；选中目标菜后进入真实单菜折扣入口，记录该入口的作用域与选中目标；在真实折扣弹窗中选择百分比、输入 10 并确认，读取折后 Subtotal 7.92；保存订单，再从 Recall 精确打开该订单并复核目标菜折扣明细与 Subtotal 7.92；
  2. 逐一展示真实折扣入口、目标菜选中态、作用域、弹窗标题和模式、百分比选择、数字输入、确认动作、保存与 Recall 转场，不得使用 `pos-ui-option-__custom_discount__` 打开的 `Custom Charge` 弹窗替代；
  3. 在最终断言所在页面保留折前 8.80、10% 折扣金额、折后 7.92 与 Recall 一致性证据，并明确结果不是 9.68 或 `Charge(10%) $0.88`。
- 请保留证据：真实单菜 Discount 入口、作用域、目标菜选中态、弹窗内稳定 data-testid/role/label、百分比模式、输入方式、确认按钮、折扣前后数值，以及保存/Recall 请求响应中的订单号、目标 itemId、折扣类型/比例/金额和最终 Subtotal。
- 当前阻塞：真实 UI 已证明现有 `pos-ui-option-__custom_discount__` 只打开正向 `Custom Charge`；10% 后 Subtotal 从 8.80 变为 9.68，现有契约不能代表 Discount，且缺少真实折扣入口、弹窗和结果 DOM 契约。
- 录制返回后计划补充：`pages/order-dishes/order-dishes-discount.section.ts`、`flows/order-dishes.flow.ts`、`tests/py-migrate/order-page-regression.spec.ts` 中的以下职责：Page 负责真实单菜折扣入口、弹窗动作与窄读取，Flow 负责编排选菜、保存和 Recall 回查，spec 断言 8.80 的 10% 折扣按产品规则得到 7.92。

### ORDER-PAGE-050：提示词 28 POS-28674 特殊价格的菜-设置50%折扣，保存订单（已自动化）

- Jira：`POS-28674`。
- 当前状态：`tests/py-migrate/order-page-regression.spec.ts` 已在真实 `Price` 弹窗输入 5.85 并选择隔离 50% 折扣；授权接口成功后弹窗仍不关闭和提交，正式用例直接 Failed，待产品修复后继续执行保存请求与 Recall 舍入断言。
- 已知前置：可从 POS 首页进入点单页、支持把目标菜改价为 5.85，以及产品真实支持的单菜 50% Discount 入口；不得把 `Custom Charge` 当作 Discount。
- 请从 POS 首页开始录制：
  1. 最小录制路径：从 POS 首页建立员工上下文，进入堂食无桌点单并添加目标菜；通过真实改价入口把目标菜改为 5.85，读取并保留改价结果；选中该菜进入真实单菜折扣入口，在真实折扣弹窗中选择百分比、输入 50 并确认，读取按产品舍入后的 Subtotal 2.92；保存订单，再从 Recall 精确打开该订单并复核目标菜改价、50% 折扣明细与 Subtotal 2.92；
  2. 逐一展示改价入口与确认、真实折扣入口、目标菜选中态、作用域、弹窗标题和模式、百分比选择、数字输入、确认动作、保存与 Recall 转场，不得使用 `Custom Charge` 弹窗替代；
  3. 在最终断言所在页面保留改价 5.85、50% 折扣金额、按产品舍入后的 2.92 与 Recall 一致性证据，并明确结果不是 8.78 Charge。
- 请保留证据：真实改价与单菜 Discount 入口、作用域、目标菜选中态、弹窗内稳定 data-testid/role/label、百分比模式、输入方式、确认按钮、折扣前后数值，以及保存/Recall 请求响应中的订单号、目标 itemId、改价、折扣类型/比例/金额和最终 Subtotal。
- 当前阻塞：把目标菜改价为 5.85 后调用现有 50% 接口，Subtotal 实际为 8.78；现有 `Custom Charge` 契约不能代表 Discount，且缺少真实 50% 折扣入口、作用域、弹窗与保存/Recall 结果契约。
- 录制返回后计划补充：`pages/order-dishes/order-dishes-discount.section.ts`、`flows/order-dishes.flow.ts`、`tests/py-migrate/order-page-regression.spec.ts` 中的以下职责：Page 负责改价后真实单菜折扣入口、弹窗动作与窄读取，Flow 负责编排改价、折扣、保存和 Recall 回查，spec 断言 5.85 的 50% 折扣按产品舍入得到 2.92。

## 不需要录制

以下项目需要配置值、产品规则确认或产品修复，不属于页面录制问题：

### 已从 `test.fail(...)` 改为普通直接失败（21 条）

以下用例仅保留“已知产品问题”报告注释，不再调用 `test.fail(...)`。断言不满足时 Playwright 会正常显示 Failed，待产品修复后继续重跑整改。

#### 手动加收配置变更（6 条）

- POS-27156：后台改名后历史手动加收应保留旧名称还是使用新名称。
- POS-27157：后台将手动固定 `$10` 加收改为 `10%` 后，重新编辑仍保留历史 `$10`，未按新配置重算为 `$0.88`。
- POS-27158：后台将手动 `10%` 加收改为固定 `$10` 后，重新编辑仍保留历史 `10%` 金额 `$0.88`。
- POS-27159：后台将手动固定加收由 `$10` 改为 `$20` 后，重新编辑仍保留历史 `$10`。
- POS-27163：后台将手动固定加收改为计税后，重新编辑税额仍为 `$0.88`，未高于修改前税额。
- POS-27165：后台将手动加收改为仅 Delivery 后，Dine In 历史订单重新编辑仍保留 `$10`，未按订单类型条件隐藏。

#### 自动加收配置变更（6 条）

- POS-27170：已改为每次运行创建隔离唯一名称；后台改名后重新编辑未显示改名后的目标自动加收，仅剩环境加收。
- POS-27171：隔离自动固定 `$10` 加收改为 `10%` 后，重新编辑未显示目标自动加收。
- POS-27172：隔离自动 `10%` 加收改为固定 `$10` 后，重新编辑未显示目标自动加收。
- POS-27173：隔离自动固定加收由 `$10` 改为 `$20` 后，重新编辑未显示目标自动加收。
- POS-27174：隔离自动百分比加收由 `10%` 改为 `20%` 后，重新编辑未显示目标自动加收。
- POS-27177：隔离自动加收改为计税后，重新编辑未显示目标自动加收。

#### 配置变更后的保存（1 条）

- POS-27192：自动加收配置修改后从编辑页保存会丢失新加收。

> POS-27248 已改为隔离唯一加收名称并于 2026-07-20 真实 UI 通过，1 passed（40.8s），已从失败清单移除。

#### 复制订单（3 条）

- POS-27257：复制自动加收订单后未显示后台更新后的加收名称和金额。
- POS-27271：Delivery 源订单保存前未生成目标自动加收 `auto_test1`，无法进入修改 `minMileage` 后复制订单的断言。
- POS-27288：创建源订单时确认目标手动加收后 Charge 弹窗未关闭且加收未生效，无法进入复制后的订单类型断言。

#### 支付后分单限制（2 条）

- POS-19365：已支付共享菜后作废另一子单时，产品未返回需求中的阻断提示，当前保持普通 Failed。
- POS-19371：半支付座位分单点击 Unsplit 后，产品未返回需求中的阻断提示，当前保持普通 Failed。

#### 合单加收（1 条）

- POS-32006：合单重新计算后未新增满足人数条件的自动服务加收。

#### 金额精度与税额配置（1 条）

- POS-32963：`100 × 1.015%` 实际显示 `$1.01`，预期 `$1.02`。

### 其他普通直接失败问题

- POS-27160：普通 Failed。重新进入编辑页时正确保留历史 `$0.88`；打开 Charge 弹窗及确认后仍为 `$0.88`，均未按后台 `20%` 配置更新为 `$1.76`。
- POS-31663 / BUG POS-50142：普通 Failed。Count 设置为 `5` 正常生效，但再次设置为 `0` 后 Confirm 按钮被禁用，无法按需求确认归零移除；Bug 修复后继续整改并跑通自动化。
- POS-35325：普通 Failed。Delay API 成功建立延迟送厨状态，但受限员工在 Recall 编辑页把目标菜 Count 设置为 `0` 后，页面未显示 `VOID_KITCHEN_ITEM` 授权提示，菜品数量仍为 `1`；用例不使用 `test.fail(...)`，产品修复后继续执行主管授权、保存和 Recall 回查。
- POS-32954：POS NG 不适用，保留 skip，不需要录制。
- POS-32016：现有实现只创建 Delivery 源单和 To Go 目标单，缺少原需求的两笔 Delivery 改价、报表 Fee 与小费校验，需重写数据构造。

## 维护规则

- 收到有效录制后，在完成开发前保留对应条目并标记录制已收到，避免重复录制。
- 用例完成开发并真实跑通后，从本文档直接移除对应条目，不保留完成记录。
- 如果失败点是产品行为或后台配置，不新增录制请求，直接记录实际结果和待确认项。
