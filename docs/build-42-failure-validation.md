# Build #42 失败用例逐条验证

验证日期：2026-07-23  
环境：`http://192.168.0.247:22080/kpos`  
范围：Allure Build #42 非礼品卡失败；按要求排除 3 条信用卡用例和 POS-34187 打印/报表用例。

## 已修复并单例通过（21 项）

| 用例 | 结果 | 修复或验证说明 |
| --- | --- | --- |
| POS-19362 | 通过 | 通过 API 关闭 `IS_SHOW_SEATS`，Add Line 可见；用例通过。 |
| POS-31664 | 通过 | 修正 Reduce 归零后的 Modify 面板最终状态断言。 |
| POS-30575 | 通过 | 客户信息定位收敛到真实页面作用域。 |
| POS-35150 | 通过 | 等待负向退款流水稳定后再断言。 |
| POS-34787 | 通过 | 按订单 ID 清两张桌、覆盖 Printed/分单状态，并按真实 `table-node-available` 选择换桌目标。 |
| POS-34827 | 通过 | 改为比较操作前后真实 Printed 数据库状态，不再写死状态枚举。 |
| POS-19368 | 通过 | 补充座位分单前置清桌并刷新桌台数据。 |
| POS-19386 | 通过 | 同上；子单减菜后 tips 重算通过。 |
| POS-19389 | 通过 | 同上；子单折扣后 tips 重算通过。 |
| POS-19517 | 通过 | 使用真实价格确认按钮及 Recall 支付退款按钮契约。 |
| POS-23322 | 通过 | 余额断言改为验证部分现金支付后精确减少 5 元，隔离环境自动加收污染。 |
| POS-27324 | 通过 | 使用唯一加收名称并修复员工登录刷新竞态。 |
| POS-27325 | 通过 | 使用唯一加收名称并修复员工登录刷新竞态。 |
| POS-32002 | 通过 | 使用唯一加收名称并修复员工登录刷新竞态。 |
| POS-43825 | 通过 | 按真实 DOM 使用 `#orderDishesRoot` 内的 `data-testid="icon-button-Back"` 退出 Modify；单例通过。 |
| POS-15534 | 通过 | 环境补充 `svip` 区域后，跨区域选择空桌、创建订单及 Recall 桌号回查均通过。 |
| POS-15557 | 通过 | 等待 1.7 分钟后返回主页，等待主页刷新完成再重新进入原桌台区域，订单时长正确显示为 `0:01`。 |
| POS-43892 | 通过 | Build #42 实际已显示库存不足弹窗，但通用保存清理误点 “I Got it” 后才读取；改为保留业务弹窗并按录制的 Dine In、双击加单击路径验证通过。 |
| POS-27259 | 通过 | 加收用例前通过 API 清空全部加收配置，独立创建 SERVICE 自动加收；最小人数由 1 改为 2 后，复制页及保存后 Recall 均无加收且合计为 9.68。 |
| POS-32008 | 通过 | 改为按真实业务路径创建两笔各含 10 元同名自动加收的 Dine In 订单；关闭合单重新计算后合并，加收正确累加为 20 元。 |
| POS-33063 | 通过 | 清空加收后确认最终页面实际为 Charge 0.97、Total 10.65；改为等待价格汇总稳定并断言最终金额，不再读取 Charge 弹窗的陈旧草稿值 0.88。 |

## 已复现的产品问题（31 项）

| 用例 | 最终失败证据 |
| --- | --- |
| POS-43898 / POS-43890 / POS-43889 | 送厨后选择“不恢复库存”，库存仍从 15 恢复到 20。未送厨的点菜/加菜/减菜分支已通过。 |
| POS-36255 | 菜名与编号相同的搜索结果显示 2 条，预期 1 条。 |
| POS-31663 | Count 已显示 0，但确认按钮仍禁用，无法完成归零移除。 |
| POS-42958 | 不参与整单折扣的类别仍计入比例加收，实际 1.88、预期 0.88。 |
| POS-35142 | 折扣授权成功后 Price 弹窗不关闭，无法继续退款链路。 |
| POS-35155 | 已退款菜品再次送厨后仍显示 `Sent in`。 |
| POS-35498 | 折扣授权成功后 Price 弹窗不关闭。 |
| POS-36140 | 折扣授权成功后 Price 弹窗不关闭。 |
| POS-36148 | 折扣授权成功后 Price 弹窗不关闭。 |
| POS-36156 | 折扣授权成功后 Price 弹窗不关闭。 |
| POS-42631 | 打印接口返回 200 后数据库状态仍为 SUBMITTED，未转为 PRINTED。 |
| POS-42086 | 折扣授权成功后 Price 弹窗不关闭。 |
| POS-42085 | 折扣授权成功后 Price 弹窗不关闭。 |
| POS-35325 | Delay 菜通过授权删除时未出现需求要求的 `VOID_KITCHEN_ITEM` 提示。 |
| POS-42886 | 折扣授权成功后 Price 弹窗不关闭。 |
| POS-28674 | 折扣授权成功后 Price 弹窗不关闭。 |
| POS-16316 | 2.00/8.60 两个未支付子单正确，但 Submit 后面板不关闭、页面不切换且无提示。 |
| POS-34463 | 打印接口返回 200 后订单未进入 Printed 筛选结果。 |
| POS-19365 | 已支付共享菜后作废另一子单，阻断提示返回 `null`。 |
| POS-19371 | 半支付后撤销分单，没有任何可读阻断提示。 |
| POS-25235 | To Go 平分子单现金支付后追加 1 元小费，最终小费仍为 0。 |
| POS-27156 | 手动加收改名后，编辑历史订单未显示新名称。 |
| POS-27157 | 固定 10 元改为 10% 后仍保留 10 元，预期 0.88。 |
| POS-27158 | 10% 改为固定 10 元后仍保留 0.88。 |
| POS-27159 | 固定 10 元改为 20 元后仍保留 10 元。 |
| POS-27160 | 百分比 10% 改为 20% 后弹窗仍显示 0.88，预期 1.76。 |
| POS-27163 | 加收改为计税后税额仍为 0.88，没有增加。 |
| POS-27165 | 加收改为仅 Delivery 后，Dine In 历史订单仍保留 10 元。 |
| POS-32963 | 1.015% 加收显示 1.01，未按规则进位为 1.02。 |
| POS-34555 | 两次现金付款且第二次产生找零后，支付状态仍为 Semi-Paid。 |
| POS-15564 | 订单已填写动态客户名，桌台卡片 Party Name 仍显示 `-`。 |

## 明确排除

- 礼品卡：全部不处理。
- 信用卡：无 Jira 编号的 Recall To Go 信用卡成功状态、POS-33122、POS-35173。
- 打印/报表：POS-34187（虽然本地打印目录问题已修复并曾单例通过，但不计入本轮范围）。

## 验证质量

- `node node_modules/typescript/bin/tsc --noEmit`：通过。
- `node --test tests/scripts/jenkinsfile-ui-scope.unit.test.mjs`：2/2 通过。
- `git diff --check`：通过。
- 清桌统一使用 `/api/order/clearTable` 的 `orderIds` 形式，并覆盖普通、Printed 和分单父子订单状态。
