# Superpowers 文档目录说明

`docs/superpowers/` 存放 AI 协作过程中的设计、规格和实施计划。

这些文件不是 Playwright 测试，也不是 API 客户端或测试数据。它们用于记录为什么这样设计、执行过哪些步骤、哪些接口暂缓或需要补抓包。

- `specs/`：需求设计和覆盖策略。
- `plans/`：实施计划和执行清单。

查找可运行 API 测试时请看 `tests/api/`；查找 API 支撑代码时请看 `api/`；查找请求体工厂时请看 `test-data/api/`。
