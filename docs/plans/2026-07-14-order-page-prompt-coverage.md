# 点单页面提示词覆盖矩阵

## 数据源

- 提示词：`C:\Users\nhqrt\Desktop\test_order_page_playwright_prompts.md`
- 设计：`docs/superpowers/specs/2026-07-14-order-page-prompt-regression-design.md`
- 基线：66 条，编号 1～66。

## 状态口径

| 状态 | 完成证据 |
|---|---|
| 已等价覆盖 | 具体 spec、中文测试标题和关键断言均与提示词等价 |
| 待补断言 | 已有路径可执行，但缺少提示词要求的核心可观察结果 |
| 可直接实现 | 真实 Page/Flow API 和稳定 DOM 契约已经存在 |
| 需要录制 | 缺真实路径、DOM、配置入口或业务预期；关联唯一录制编号 |
| 产品异常 | 已有可重复执行证据证明实际结果与需求冲突 |
| 环境阻塞 | 缺账号、权限、数据、打印目录或外部服务，并记录恢复条件 |

Tasks 2～5 已完成状态分类和逐行录制证据定义；Task 6 已按全矩阵顺序为所有 `需要录制` 行生成正式录制编号与一对一录制条目。

## 录制落地批次 3

- 执行日期：`2026-07-20`。
- `ORDER-PAGE-010`、`ORDER-PAGE-012`、`ORDER-PAGE-013`、`ORDER-PAGE-017`、`ORDER-PAGE-023` 已依据录制补齐并逐条通过真实 UI。
- `ORDER-PAGE-014` 已按实测补正支付成功页契约：按钮视觉文案为 `NO RECEIPT`（DOM 文本 `No Receipt`），可点击关闭且页面也会自动关闭；目标用例已通过真实 UI。
- 当前状态汇总为 `28/0/0/38/0/0`（已等价覆盖/待补断言/可直接实现/需要录制/产品异常/环境阻塞）。
- `docs/playwright-recordings-needed.md` 已移除这 5 条录制请求，剩余 38 条，编号保持原映射、不重编号。

## 录制落地批次 2

- 执行日期：`2026-07-20`。
- `ORDER-PAGE-016` 已依据录制补齐并通过真实 UI；`ORDER-PAGE-014` 已收到录制，但在按稳定 DOM 契约实现时确认存在 Split iframe 持续拦截，转为环境阻塞。
- 当前状态汇总为 `22/0/0/43/0/1`（已等价覆盖/待补断言/可直接实现/需要录制/产品异常/环境阻塞）。
- `docs/playwright-recordings-needed.md` 已移除这 2 条录制请求，剩余 43 条，编号保持原映射、不重编号。

## 录制落地批次 1

- 执行日期：`2026-07-15`。
- `ORDER-PAGE-002`～`ORDER-PAGE-006` 已依据录制补齐并逐条通过真实 UI；状态汇总更新为 `21/0/0/45/0/0`。
- `docs/playwright-recordings-needed.md` 已移除这 5 条录制请求，剩余编号保持原映射、不重编号。
- 同批收到的订单操作场景 `POS-27242` 也已落地并通过，已从订单操作录制清单移除。

## 批次 0 验证

- 执行日期：`2026-07-14`
- 矩阵基线：66 行，编号 1～66 无缺口，状态值全部合法；状态汇总为 `16/0/0/50/0/0`（已等价覆盖/待补断言/可直接实现/需要录制/产品异常/环境阻塞）。
- 证据完整性：已等价覆盖但无 spec 行号证据 `0`，需要录制但无录制编号 `0`，含模糊“环境不可用”阻塞 `0`。
- 录制映射：矩阵中 50 个唯一录制编号与 `docs/playwright-recordings-needed.md` 的 50 个唯一条目双向完全一致，缺失 `0`，额外 `0`，编号集合连续为 `ORDER-PAGE-001`～`ORDER-PAGE-050`；既有 `ORDER-PAGE-001`～`ORDER-PAGE-048` 保持原矩阵顺序，两条重新分类场景追加为 `ORDER-PAGE-049`、`ORDER-PAGE-050`。
- 提示词重复性：点单页面源文件 66 条正文完全重复组 `0`；对点单页面 66 条与点单操作 95 条正文统一换行、去首尾空白、合并连续空白并转小写后，文件内重复组均为 `0`，跨文件重复正文为 `0`。

### 矩阵行数、编号与状态集合

```powershell
$matrix = Get-Content -Raw -LiteralPath 'docs\plans\2026-07-14-order-page-prompt-coverage.md' -Encoding UTF8
$rows = [regex]::Matches($matrix, '(?m)^\| (?<n>\d+) \| (?:(?:POS-\d+)(?:,\s*POS-\d+)*|—) \|(?<body>.+)$')
$numbers = @($rows | ForEach-Object { [int]$_.Groups['n'].Value })
$allowed = '已等价覆盖|待补断言|可直接实现|需要录制|产品异常|环境阻塞'
$invalid = @($rows | Where-Object { $_.Groups['body'].Value -notmatch "\| ($allowed) \|" })
$statusCounts = @($allowed -split '\|' | ForEach-Object { @($rows | Where-Object { $_.Value -match "\| $_ \|" }).Count })
[pscustomobject]@{
  Rows = $rows.Count
  NumberingIssues = @(Compare-Object (1..66) $numbers).Count
  InvalidStatuses = $invalid.Count
  StatusCounts = $statusCounts -join '/'
}
```

退出码 `0`；观测结果：`Rows=66`、`NumberingIssues=0`、`InvalidStatuses=0`、`StatusCounts=16/0/0/50/0/0`。

### 状态证据完整性

```powershell
$rows = Get-Content -LiteralPath 'docs\plans\2026-07-14-order-page-prompt-coverage.md' -Encoding UTF8 | Where-Object { $_ -match '^\| (?<n>\d+) \| (?:(?:POS-\d+)(?:,\s*POS-\d+)*|—) \|' }
$invalidCovered = @($rows | Where-Object { $_ -match '\| 已等价覆盖 \|' -and $_ -notmatch 'tests/py-migrate/.+\.spec\.ts:\d+' })
$invalidRecording = @($rows | Where-Object { $_ -match '\| 需要录制 \|' -and $_ -notmatch 'ORDER-PAGE-\d{3}' })
$vagueEnvironment = @($rows | Where-Object { $_ -match '\| 环境阻塞 \|' -and $_ -match '环境不可用' })
$recordings = Get-Content -Raw -LiteralPath 'docs\playwright-recordings-needed.md' -Encoding UTF8
$entries = [regex]::Matches($recordings, '(?ms)^### ORDER-PAGE-\d{3}：提示词 \d+ .+?\r?\n(?<body>.*?)(?=^### ORDER-PAGE-|^## 已收到，无需重复录制)')
$invalidEntries = @($entries | Where-Object {
  $_.Groups['body'].Value -notmatch '(?m)^- Jira：' -or
  $_.Groups['body'].Value -notmatch '(?m)^- 已知前置：' -or
  $_.Groups['body'].Value -notmatch '(?m)^- 请从 POS 首页开始录制：' -or
  $_.Groups['body'].Value -notmatch '(?m)^- 请保留证据：' -or
  $_.Groups['body'].Value -notmatch '(?m)^- 当前阻塞：' -or
  $_.Groups['body'].Value -notmatch '(?m)^- 录制返回后计划补充：'
})
[pscustomobject]@{
  CoveredWithoutSpecEvidence = $invalidCovered.Count
  RecordingWithoutId = $invalidRecording.Count
  VagueEnvironmentBlockers = $vagueEnvironment.Count
  InvalidRecordingEntries = $invalidEntries.Count
}
```

退出码 `0`；观测结果：`CoveredWithoutSpecEvidence=0`、`RecordingWithoutId=0`、`VagueEnvironmentBlockers=0`、`InvalidRecordingEntries=0`。

### 录制编号双向映射与编号集合

矩阵的 50 条 `需要录制` 行与录制文档的 50 个条目分别提取编号、提示词编号、Jira 和标题，检查 `ORDER-PAGE-001`～`ORDER-PAGE-050` 唯一连续集合及双向对应。退出码 `0`；观测结果：矩阵编号问题 `0`、录制编号问题 `0`、提示词/Jira/标题映射问题 `0`、双向映射问题 `0`；既有 `ORDER-PAGE-001`～`ORDER-PAGE-048` 的 Jira、标题和编号映射变化 `0`。

### 标题与完整正文 Jira 对应

```powershell
$source = Get-Content -Raw -LiteralPath 'C:\Users\nhqrt\Desktop\test_order_page_playwright_prompts.md' -Encoding UTF8
$cases = [regex]::Matches($source, '(?ms)^## (?<n>\d+)\. (?<title>.+?)\r?\n(?<body>.*?)(?=^## \d+\. |\z)')
$matrixRows = [regex]::Matches($matrix, '(?m)^\| (?<n>\d+) \| (?<jira>(?:(?:POS-\d+)(?:,\s*POS-\d+)*|—)) \| (?<title>.*?) \|')
# 对每条 `$case.Value`（标题和完整正文）提取全部唯一 POS Key，再与矩阵 Jira 集合比较；录制条目的 Jira 同步与对应矩阵行比较。
```

退出码 `0`；观测结果：`SourceCases=66`、`MatrixRows=66`、标题不一致 `0`、完整正文 Jira 不一致 `0`、`RecordingEntries=50`、录制 Jira 不一致 `0`。

### 两个提示词源的完全与规范化重复正文

```powershell
$pageSource = 'C:\Users\nhqrt\Desktop\test_order_page_playwright_prompts.md'
$operationSource = 'C:\mansuper\test_order_operation_playwright_prompts.md'
# 两个源分别按完整正文和“统一换行、去首尾空白、合并连续空白、转小写”正文分组，并对两个集合做完全及规范化交叉比较。
```

退出码 `0`；观测结果：点单页面 `66` 条、点单操作 `95` 条；两个源各自的完全重复组和规范化重复组均为 `0`，跨文件完全重复与规范化重复均为 `0`。

### 仓库静态验证

```powershell
npx tsc --noEmit
```

Windows 上实际以 `npx.cmd tsc --noEmit` 执行；退出码 `0`，无 TypeScript 错误。

```powershell
npm.cmd run test:scripts
```

退出码 `0`；`3 passed`、`0 failed`。

```powershell
git diff --check c28654c..HEAD
git diff --check
```

两个命令退出码均为 `0`，无空白错误。

### 后续批次入口

- 唯一范围入口为本矩阵 66 行；后续实现按矩阵编号追踪状态与证据。
- 当前 57 条 `已等价覆盖` 作为回归基线；共享保存 helper 在保存后通过 `EmployeeLoginFlow.enterEmployeeContext` 恢复或确认员工上下文，再进入 Recall。
- 当前仅剩报表相关 POS-22657 按用户决定暂缓开发；8 条已完成自动化但受产品或环境缺陷阻塞的用例保留直接失败或已确认的失败证据，修复后直接重跑整改。

## 汇总

| 已等价覆盖 | 已自动化但当前失败 | 暂缓开发 | 总数 |
|---:|---:|---:|---:|
| 57 | 8 | 1 | 66 |

## 明细

| 编号 | Jira | 标题 | 业务域 | 状态 | 覆盖或缺口证据 | 计划落点 | 录制编号 | 最近验证 |
|---:|---|---|---|---|---|---|---|---|
| 1 | POS-15602 | POS-15602 点单页面菜单---组---切换组 | 菜单与语言 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:82；[POS-15602] 应能切换菜单组并保存目标组菜品；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesPage.switchMenuGroup/switchMenu/readSelectedMenuGroupName/clickDish/readOrderedItems/readPriceSummary/saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome` → `RecallPage.openOrderDetails/readOrderDetailsSnapshot`；关键断言：无条件切到 `Group 001` 后再切回目标菜单组、Recall 菜名一致且保存前后 Subtotal 一致 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 2 | POS-15605 | POS-15605 点单页面菜单--组 中文展示 | 菜单与语言 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:471`；[POS-15605 POS-15737] 切换中文后应展示中文菜单组和类别。主页语言按钮实测为英中双态切换，不存在语言弹层；测试先读取 `Dine In` 状态并切换为 `堂吃`，通过稳定功能卡 test id 进入 To Go，断言当前菜单组为配置的中文名称 `自动化菜单组`，退出后 finally 切回英文并断言恢复成功 | `pages/home.page.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-001` | 2026-07-20：TypeScript 检查通过；与 POS-15737 联合真实 UI 正式用例通过，1 passed（11.8s） |
| 3 | POS-15641 | POS-15641 点单页面菜单--类--组下多个类时，进行类切换 | 菜单与语言 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:277`；[POS-15641] 应能 To Go 点另一个分类菜品后在 Recall 校验菜品名称和价格；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesFlow.addRegularDish/switchMenu` → `OrderDishesPage.saveOrder` → `RecallFlow.viewFirstVisibleOrderDetails`；关键断言：切换到目标菜单分类后添加目标菜，Recall 回显菜名和价格与点单页一致 | 无需新增 | — | 2026-07-15：录制落地后真实 UI 通过 |
| 4 | POS-30543 | Seperate the same dishes开关开启，订单送厨后编辑页面，选择菜品，点击加1，存单后检查订单上的税率正确 | 送厨与合并菜 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:171`；[POS-30543] 应能堂食保存后在 Recall 编辑菜品数量并校验税额实时更新；Page/Flow 链：`apiSetup.systemConfiguration.updateByName` → `SelectTableFlow.skipTableSelectionAndEnterOrderDishes` → `OrderDishesPage.saveOrderWithReference` → `RecallFlow.editOrder`；关键断言：精确订单回查总数量由 1 增至 2、Subtotal 由 9.90 增至 19.80、Tax 由 0.99 增至 1.98，finally 恢复开关 | 无需新增 | — | 2026-07-15：录制落地后真实 UI 通过 |
| 5 | POS-42889 | 后台点单前确认客户信息，客户姓名必填，电话必填，点支付按钮后弹出输入电话号码和客户姓名的弹框 | 客户信息 | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts:23`；[POS-42889] 应能在支付前校验客户姓名和电话必填并完成现金支付；Page/Flow 链：`apiSetup.systemConfiguration.updateManyByName` → `OrderCustomerFlow.validateRequiredInformationAndOpenPayment` → `PaymentFlow.payByCash`；关键断言：空值先后触发姓名、电话必填提示，完整填写后进入 Payment 并完成现金支付，finally 恢复三个配置 | 无需新增 | — | 2026-07-15：录制落地后真实 UI 通过 |
| 6 | POS-39750 | 没有删菜权限的用户点单后删菜，弹出权限提示框提示没有权限，输入正确的密码可正常删菜 | 权限与 Void | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts:73`；[POS-39750] 应能拦截无送厨删菜权限的员工并通过主管口令完成删菜；Page/Flow 链：`apiSetup.staff.createWithoutKitchenVoidPermission` → `OrderDishesPage.sendOrderWithReference` → `RecallFlow.editOrder` → `OrderPermissionFlow.removeSentDishWithAuthorization`；关键断言：临时 Server 员工无 `VOID_KITCHEN_ITEM` 时显示权限提示，主管口令授权后 Recall 目标菜显示 `VOIDED`，测试员工自动清理 | 无需新增 | — | 2026-07-15：录制落地后真实 UI 通过 |
| 7 | POS-42886 | 菜品操作改价时可以选择单菜折扣 | 改价折扣备注 | 已自动化（当前失败） | API 创建隔离 10% 折扣后，真实 `Price` 弹窗会显示稳定折扣选项 `pos-ui-option-{discountId}`；正式用例选择该折扣并使用主管口令授权，`POST /kpos/api/login` 返回 HTTP 200、`code=0`，但产品仍保留 `Price` 弹窗且未提交修改。用例直接 Failed，不使用 skip 或预期失败；产品修复后将继续断言 Subtotal 7.92、保存请求折扣字段及 Recall 一致性 | `pages/order-dishes/order-dishes-discount.section.ts`、`pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes.page.ts`、`flows/order-dishes.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order-page-regression.spec.ts:548` | `ORDER-PAGE-049` | 2026-07-20：真实折扣入口已定位并完成自动化；当前失败点为授权成功后 Price 弹窗不关闭 |
| 8 | POS-42888 | 点Modify可以输入菜品的备注，保存订单后，菜品后面的备注信息正确 | 改价折扣备注 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:300；[POS-42888] 应能通过 Modify 添加备注并在 Recall 保留；Page/Flow 链：`SelectTableFlow.enterDineInNoTableOrder` → `OrderDishesFlow.addRegularDish/addCustomModifier` → `OrderDishesPage.saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome` → `RecallPage.openOrderDetails/readOrderDetailsSnapshot`；关键断言：按本次保存请求的精确订单号打开 Recall，目标菜 additions 包含 `POS-42888` 备注 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 9 | POS-39762 | 点单，加小费，平分订单，合并子单，订单小费金额正常 | 分单与小费 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:685；[POS-39762] 应能平分含 2.00 小费的 To Go 订单后再合并并恢复母单小费；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesFlow.addRegularDish` → `OrderDishesPage.addTip/saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome/openSplitOrder` → `SplitOrderFlow.splitOrderEvenly/submitAndReturnPage/combineSuborders` → `RecallPage.readDisplayedOrderPriceSummary`；关键断言：两个子单 Tips 均为 1.00 且合计 2.00，合并后母单 Tips 恢复 2.00 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 10 | POS-42011 | open Food 点单，不选择任何税，可以成功完成付款 | Open Food | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts:125`；[POS-42011] 应能添加不选择税的 Open Food 菜品并完成现金支付；Page/Flow 链：`SelectTableFlow.enterDineInNoTableOrder` → `OrderDishesFlow.addOpenFoodItemWithoutTax` → `OrderDishesPage.readTaxAmount/openPayment` → `PaymentFlow.payByCash`；关键断言：确认无税告警后订单 Tax 为 0，并成功完成现金支付；用例临时关闭无关客户确认配置且 finally 恢复 | 无需新增 | — | 2026-07-15：录制落地后真实 UI 通过 |
| 11 | POS-42943 | 连续创建两个不输入姓名的pick up订单，修改其中一个食客姓名，另一个不受影响 | 客户信息 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:529`；[POS-42943] 修改一笔匿名 Pick Up 订单的客户姓名不应影响另一笔订单。测试连续创建两笔不填写姓名的 Pick Up，分别添加不同菜品并从保存接口读取精确订单号；Recall 按第一笔订单号进入编辑页，只为该订单补充唯一姓名和电话并保存，再按两个精确订单号分别回查。关键断言：第一笔 customer name 等于运行期唯一姓名，第二笔 customer name 仍为空 | `test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-007` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（17.4s） |
| 12 | POS-15643 | POS-15643 点单页面菜单--类，带option的类 | Option | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1463`；[POS-15643 POS-15758] 应能在分类菜品上选择 option 和二级 option 并在 Recall 正确回显；关键断言：点单页与 Recall 的菜品 additions 均等于 `[free option, free suboption]`，Recall 菜品价格等于点单页价格且订单仅有该菜 | 无需新增 | — | 2026-07-20：POS-15759 已拆为独立跳过二级用例，本行保留完整一级+二级覆盖 |
| 13 | POS-15737 | POS-15737 点单界面菜单--类，类的中文展示 | 菜单与语言 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:471`；[POS-15605 POS-15737] 切换中文后应展示中文菜单组和类别。测试切换主页为中文，通过与语言无关的稳定功能卡 test id 进入 To Go，读取当前选中类别并断言为配置的中文名称 `全类型类`；finally 从空订单无提示退出并把主页恢复英文，避免语言状态污染后续用例 | `pages/home.page.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-008` | 2026-07-20：TypeScript 检查通过；与 POS-15605 联合真实 UI 正式用例通过，1 passed（11.8s） |
| 14 | POS-15758 | POS-15758 点单界面菜单--option 创建类的option,包含二级option | Option | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1404`；[POS-15643 POS-15758] 应能在分类菜品上选择 option 和二级 option 并在 Recall 正确回显；关键断言：点单页与 Recall 的菜品 additions 均等于 `[free option, free suboption]`，Recall 菜品价格等于点单页价格且订单仅有该菜 | 无需新增 | — | 2026-07-20：按一级+二级完整链路保留 POS-15643/POS-15758，POS-15759 拆为独立跳过二级用例 |
| 15 | POS-15759 | POS-15759 点单界面菜单--option 创建类的option,包含二级option，点单时选择了类的option，未选二级option | Option | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1383`；[POS-15759] 应能选择分类一级 option 并跳过二级 option 后在 Recall 正确回显。测试从 To Go 添加分类菜品 `普通菜2`，选择分类一级 option `free option` 后不选择可见的 `free suboption`；保存并按精确订单回查，断言点单页和 Recall additions 都严格等于 `[free option]`、订单仅包含本次菜品且价格保持一致 | `tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-009` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（9.9s） |
| 16 | POS-15760 | POS-15760 点单界面菜单--option 创建菜的option,包含二级option，点单时选择了菜的option，未选二级option | Option | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1141`；[POS-15760] 应能只选择菜品一级选项并跳过二级选项后在 Recall 正确回显；使用真实菜品 `普通菜1`，精确选择其菜品级一级 option `A`，确认二级 `a1` 可见但不选择；保存并按运行期订单号回查，断言点单页和 Recall 均仅回显 `A`，菜品价格均为 `9.8` | `pages/order-dishes/order-dishes-menu.section.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`pages/recall/recall-order-details.dialog.ts`、`flows/order-dishes.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-010` | 2026-07-20：与 POS-15759 分离业务绑定类型后，真实 UI 正式用例通过，1 passed（13.0s） |
| 17 | POS-15761 | POS-15761 点单界面菜单--option 创建菜的option，点单时选择了菜的option | Option | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1261`；[POS-15761 POS-15763] 应能选择菜品一级和二级选项并在 Recall 正确回显。Atlassian 原始标题仅要求选择菜品直接绑定的 option 并保存成功，没有“单层”约束；后台只读确认普通菜1（ID 698）直接拥有一级 `A`、二级 `a1`，而普通菜2 的 `option1` 并非菜品直接绑定。正式用例选择 `A/a1`，断言点单页 additions、价格 `9.8`，保存后按精确订单号在 Recall 断言两级 option 与价格均一致 | `test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-011` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（11.0s） |
| 18 | POS-15762 | POS-15762 点单界面菜单--option 创建菜的option，点单时未选择菜的option | Option | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts`：[POS-15762] 应能不选择菜品选项直接保存并在 Recall 保持无选项；打开真实 option 面板后不选择任何 option，直接保存并按精确订单号回查；关键断言：点单页和 Recall 均无菜品 option，菜品价格保持基础价 `8.8` | `pages/order-dishes/order-dishes-menu.section.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`pages/recall/recall-order-details.dialog.ts`、`flows/order-dishes.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-012` | 2026-07-20：真实 UI 与 POS-15760/POS-15763 联合通过，3 passed |
| 19 | POS-15763 | POS-15763 点单界面菜单--option 创建菜的option,包含二级option，点单时选择了菜的option，和二级option | Option | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts`：[POS-15763] 应能选择菜品一级和二级选项并在 Recall 正确回显；按真实层级依次选择 `A`、`a1` 并保存；关键断言：点单页和 Recall 均回显两级 option 且菜品价格为 `9.8`，回查使用运行期精确订单号 | `pages/order-dishes/order-dishes-menu.section.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`pages/recall/recall-order-details.dialog.ts`、`flows/order-dishes.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-013` | 2026-07-20：真实 UI 与 POS-15760/POS-15762 联合通过，3 passed |
| 20 | POS-16303 | POS-16303 点单页面订单-订单底部功能--平分订单 | 分单与小费 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:347；[POS-16303] 应能保存 To Go 订单后从 Recall 平分两份并保持金额守恒；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesFlow.addRegularDish` → `OrderDishesPage.saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome/openSplitOrder` → `SplitOrderFlow.splitOrderEvenly/submitAndReturnPage` → `SplitOrderPage.readSnapshot`；关键断言：两个未支付子单各为母单金额一半，且整数分合计等于订单总额 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 21 | POS-16314 | POS-16314 点单页面订单-订单底部功能--拖动分单 | 分单与小费 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:397；[POS-16314] 应能保存两道不同菜品后从 Recall 将普通菜2移入新子单；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesFlow.addRegularDish` → `OrderDishesPage.saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome/openSplitOrder` → `SplitOrderFlow.moveDishToNewSuborder/submitAndReturnPage` → `SplitOrderPage.readSnapshot`；关键断言：普通菜2 从源单移出并只进入目标子单，共 2 个未支付子单且整数分金额守恒 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 22 | POS-16315 | POS-16315 点单页面订单-订单底部功能--按座位分单 | 分单与小费 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:575；[POS-16315] 应能保存共享座位与座位一菜品后从 Recall 按座位分单；Page/Flow 链：`HomePage.enterDineInEntry` → `SelectTableFlow.selectAnyAvailableTableAndEnterOrderDishes` → `OrderDishesPage.selectSharedSeat/selectSeat/saveOrderWithReference` → `OrderDishesFlow.addRegularDish` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome/openSplitOrder` → `SplitOrderFlow.splitOrderBySeats/submitAndReturnPage` → `SplitOrderPage.readSnapshot` → `RecallFlow.cleanupPersistedSplitOrder`；关键断言：两个未支付子单的 `Shared`/`Seat 1` 与菜品归属正确，整数分金额合计等于分单前总额，并在 finally 清台作废 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 23 | POS-16316 | POS-16316 点单页面订单-订单底部功能--自定义分单 | 分单与小费 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:473；[POS-16316] 应能保存改价为 10.60 的订单后从 Recall 按 2 和 8.6 分单；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesFlow.addRegularDish` → `OrderDishesPage.changeOrderedDishPrice/saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome/openSplitOrder` → `SplitOrderFlow.splitOrderByAmounts/submitAndReturnPage` → `SplitOrderPage.readSnapshot`；关键断言：两个未支付子单金额依次为 2 和 8.6 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 24 | POS-16318 | POS-16318 点单页面订单-订单底部功能--分单 撤销分单 | 分单与小费 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:515；[POS-16318] 应能保存订单并提交平分后从 Recall 撤销分单恢复总额；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesPage.saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome/openSplitOrder` → `SplitOrderFlow.splitOrderEvenly/submitAndReturnPage/cancelSplit` → `SplitOrderPage.readSnapshot`；关键断言：提交后按精确单号重开并撤销，恢复为 1 个未支付子单且整数分总额等于分单前 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 25 | POS-16325 | POS-16325 点单页面订单-订单底部功能--分单，平分菜 | 分单与小费 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:770；[POS-16325] 应能保存两人堂食两菜订单后从 Recall 对普通菜1执行 Even Item；Page/Flow 链：`HomePage.enterDineIn` → `SelectTableFlow.selectAnyAvailableTableAndEnterOrderDishes` → `OrderDishesFlow.addRegularDish` → `OrderDishesPage.saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome/openSplitOrder` → `SplitOrderFlow.evenSplitDishOnSuborder/submitAndReturnPage` → `SplitOrderPage.readSnapshot` → `RecallFlow.cleanupPersistedSplitOrder`；关键断言：目标菜比例为两个 `1/2` 且子单未支付，并在 finally 清台作废 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 26 | POS-16324 | POS-16324 点单页点击分单后支付部分子单，查看订单信息 | 分单与小费 | 已等价覆盖 | `tests/py-migrate/order-page-regression.spec.ts`：[POS-16324] 应能在分单页现金支付一个子单并查看两个子单状态；从 To Go 点单页进入 Split，对 `普通菜1` 执行 Even Item 两份并把一份移入新子单，通过录制的 `payBtn-1` 支付第一个子单，现金支付成功页精确使用 `pay-success-status-button-1`，校验其视觉文案 `NO RECEIPT` 后点击关闭；关键断言：支付前两个子单等额且总额守恒，支付后一个子单为 Paid、另一个仍未支付且总额继续守恒。按实测确认，支付成功页不点击也约 3 秒自动关闭；最终退出/Recall 转场不是本用例主要校验点，不作为覆盖判定条件 | `pages/payment.page.ts`、`pages/split-order.page.ts`、`flows/payment.flow.ts`、`flows/split-order.flow.ts`、`tests/py-migrate/order-page-regression.spec.ts` | `ORDER-PAGE-014` | 2026-07-20：真实 UI 通过，1 passed（13.8s） |
| 27 | — | test_open_food_keyboard_multi_language | Open Food | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:876`；[ORDER-PAGE-015] Open Food 屏幕键盘应能切换中文并输入候选字；通过真实配置 `DEFAULT_KEYBOARD_TYPE=0` 保证默认键盘布局并在 finally 恢复，刷新后进入 To Go 打开 Open Food；断言屏幕键盘语言从 `EN` 切到“中”，逐个点击真实 test id 键 `n`、`i`，再点击语义候选按钮“你”，断言 Name 输入值和最终订单行均为“你”并保存；中文名称没有使用 `fill` 绕过屏幕键盘 | `api/setup/system-configuration.setup.ts`、`test-data/order-service.ts:107`、`pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-menu.section.ts:459`、`tests/py-migrate/order.service.spec.ts:876` | — | 2026-07-20：真实 UI 1/1 通过（1.1 分钟），TypeScript 编译通过 |
| 28 | POS-28674 | POS-28674 特殊价格的菜-设置50%折扣，保存订单 | 改价折扣备注 | 已自动化（当前失败） | 关联缺陷 POS-28534 明确要求特殊价格菜品的百分比折扣可保存。正式用例在真实 `Price` 弹窗输入 5.85、选择 API 创建的 50% 折扣并完成主管授权；授权接口成功后产品仍不关闭弹窗和提交修改。用例直接 Failed，修复后将继续断言保存请求中的特殊价格/折扣字段及 Recall Subtotal 2.92 | `pages/order-dishes/order-dishes-discount.section.ts`、`pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes.page.ts`、`flows/order-dishes.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order-page-regression.spec.ts:619` | `ORDER-PAGE-050` | 2026-07-20：TypeScript 检查通过；两条真实 UI 用例均稳定失败于授权成功后 Price 弹窗不关闭 |
| 29 | POS-30575 | POS-30575：delivery点单，输入用户信息，进入点单页面点击info,info信息预输入的一致 | 客户信息 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts`：[POS-30575] 应能在 Delivery 点单页 Info 和 Recall 中展示一致的客户信息；使用固定数据 name=`baga`、phone=`9322222222`、address=`huashengdun`、Apt=`123`、note=`POS-30575 客户信息预填`。点单页 Info 直接断言姓名、格式化电话和地址；保存前读取窄地址摘要并断言 Apt；保存后按精确订单号进入 Recall，断言姓名、电话、地址、Apt 与备注完整回显 | `pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-customer.section.ts`、`pages/delivery.page.ts`、`flows/takeout.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-016` | 2026-07-20：真实 UI 通过，1 passed（12.0s）；TypeScript 检查通过 |
| 30 | POS-31045 | POS-31045：选择的套餐子菜包含多个option，点单页面连续删除option正常 | Modify | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts`：[POS-31045] 应能连续删除套餐子菜选项并在 Recall 保留剩余选项；使用录制中的套餐 `套餐1`、子菜 `普通菜1` 和精确组合菜 DOM ID，初始选择 `A`、`a1`、`option1`、`free option`，连续通过真实 Reduce 控件删除 `a1` 与 `A`；关键断言：每步目标 option 消失而子菜保留，最终点单页与 Recall 仅保留 `option1`、`free option`，价格从 `31.6` 变为 `30.6` | `pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`pages/recall/recall-order-details.dialog.ts`、`flows/order-dishes.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-017` | 2026-07-20：真实 UI 通过，1 passed |
| 31 | POS-30762 | POS-30762：切换pos菜单模式后，搜索检查，默认搜索菜品 Broccoli Garlic Sauce | 菜单与搜索 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:777`；[POS-30762] 切换 POS 与 EMENU 菜单模式后应能搜索对应菜单菜品；读取并通过真实配置键 `MENU_PRODUCT_MODE` 在 POS/EMENU 间切换，同时开启 `SEARCH_MENU`，分别为 POS Menu 与 Emenu Menu 隔离预置 `Broccoli Garlic Sauce`、`All you can eat item`；每次刷新 POS 后进入 To Go，以单一搜索输入契约断言对应模式的精确菜品按钮可见；finally 恢复两项配置并刷新，资源注册表清理临时菜单数据 | `api/setup/system-configuration.setup.ts`、`test-data/order-service.ts:72`、`pages/order-dishes/order-dishes-menu.section.ts:156`、`tests/py-migrate/order.service.spec.ts:777` | — | 2026-07-20：Atlassian summary 为“切换pos菜单模式后，可以搜索对应菜单的菜”；真实 UI 1/1 通过（1.5 分钟），TypeScript 编译通过 |
| 32 | POS-31662 | POS-31662：点单页面，选择菜品，点击modify，添加任意global option，点击add，新增数量成功，页面右侧继续展示modify页面 | Modify | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1956`；正式用例进入 To Go，添加普通菜1并在 Modify 选择全局选项“加柴”，读取初始数量 1，点击真实 Add 后断言数量变为 2，并对选择前后及 Add 后三个阶段逐一断言 Modify 面板持续可见。公共点单页 Back 定位已按真实 DOM 收敛到 `button-back`，避免 Modify 内 `icon-button-Back` 造成 strict-mode 冲突 | `pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-modifier.section.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`flows/order-dishes.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-019` | 2026-07-20：真实 UI 通过，1 passed（10.3s） |
| 33 | POS-31663 | POS-31663：点单页面，选择菜品，点击modify，添加任意global option，点击count，增加数量，新增数量成功，页面右侧继续展 示modify页面，点击count修改数量为0，点单列表不再展示该global option，页面右侧继续展示modify页面 | Modify | 已自动化（当前失败） | `tests/py-migrate/order.service.spec.ts:2000`；正式用例已覆盖选择“加柴”、Count 设置为 5、数量与 Modify 面板断言，再次通过真实 Count 弹窗输入 0。当前产品在显示值为 0 时禁用 Confirm，无法提交归零删除；用例未使用 `test.fail(...)`，直接失败在 Confirm 应可用断言，关联 Bug POS-50142 | `pages/order-dishes/order-dishes-modifier.section.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`flows/order-dishes.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-020` | 2026-07-20：真实 UI 稳定复现，Expected enabled / Received disabled，1 failed（16.5s） |
| 34 | POS-31664 | POS-31664：点单页面，选择菜品，点击modify，添加任意global option，点击count，增加数量为2，页面右侧继续展示modify页 面，点击reduce，页面右侧继续展示modify页面，减少至0，点单列表不再展示该global option，页面右侧继续展示modify页面 | Modify | 已自动化（当前失败） | `tests/py-migrate/order.service.spec.ts:2050`；正式用例已通过 Count 把“加柴”设为 2，再连续点击真实 Reduce，数量轨迹严格断言为 1→2→1→0，且归零后订单行已移除选项。当前产品在数量归零时同时关闭 Modify 面板，最终面板可见性实际为 false；用例未使用 `test.fail(...)`，直接失败在面板持续可见断言，关联 Bug POS-50141 | `pages/order-dishes/order-dishes-modifier.section.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`flows/order-dishes.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-021` | 2026-07-20：真实 UI 稳定复现，Expected true / Received false，1 failed（11.1s） |
| 35 | POS-31409 | 下单页和订单详情卡片上展示用户信息 | 客户信息 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1494`；正式用例从无桌堂食进入点单页，通过真实 Customer 入口录入动态唯一姓名和手机号，先断言点单页客户按钮回显，再保存并在 Recall 详情卡片断言姓名、手机号，最后点击 Edit 回到点单页客户信息页面，再次断言姓名和标准化手机号均与输入一致 | `pages/order-dishes/order-dishes-customer.section.ts`、`pages/recall/recall-order-details.dialog.ts`、`flows/order-customer.flow.ts`、`flows/recall.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-022` | 2026-07-20：真实 UI 通过，1 passed（10.8s） |
| 36 | POS-33447, POS-33456 | POS-33447：Search Menu设置关闭，进入点单页面，页面不再展示搜索输入框，页面展示无违和 POS-33456：Search Menu设置开启，recall进入订单编辑页面，页面展示搜索输入框，可正常搜索，默认搜索菜品Broccoli Garlic Sauce | 菜单与搜索 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts`：[POS-33447 POS-33456] 应能按配置控制新订单与 Recall 编辑页的菜单搜索入口；通过配置 API 读取并在 finally 恢复 `SEARCH_MENU`，真实验证开启态可搜索 `superman` 并选择精确结果 `superman item4`、关闭态入口不可见、重新开启后从 Recall 精确订单进入 Edit 仍可搜索并选择同一结果 | `api/setup/system-configuration.setup.ts`、`pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`flows/recall.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-023` | 2026-07-20：真实 UI 通过，1 passed |
| 37 | POS-32905 | POS-32905 点单，菜的总数累加为整数，count不带小数点，保存订单成功 | 数量 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:129；[POS-32905] 应能以整数原文展示累计菜品数量并保存；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesFlow.addDishToCart/addRegularDish` → `OrderDishesPage.readCountText/saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome` → `RecallPage.openOrderDetails/readOrderDetailsSnapshot`；关键断言：点单页 Count 原文为 `4`，按精确单号回查后两菜数量原文为 `3`、`1` | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 38 | POS-33110 | POS-33110 pos点单，点单页面加小费，输入的小费金额大于订单total的50%，弹框提示，点击yes，加小费成功 | 支付与小费 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:613`；[POS-33110] 根据 Total Before Tips 动态生成超过 50% 的小费；helper 只有在真实大额小费弹框可见时才进入确认并点击 Yes，保存后 Recall 断言 Tips 等于输入金额。测试虽比较返回文案，但 `confirmBigTipDialog` 在读取失败时会回退预期字符串，因此该比较不能作为真实提示文案证据；提示词只要求出现提示、点击 Yes 和加费成功，现有可见性分支、真实 Yes 动作与最终 Tips 足以覆盖 | 无需新增 | — | 2026-07-14：复核 helper 文案回退后，仅以弹框分支、Yes 与最终 Tips 判定覆盖 |
| 39 | POS-33122 | POS-33122 pos点单，信用卡全额付款，付款后加小费，输入的小费金额大于订单total的50%，弹框提示，点击yes，加小费成功 | 支付与小费 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:656`；[POS-33122] 保存 To Go、信用卡全额支付并按订单号重开 Paid 订单，在 PAYMENT 卡片输入超过 50% 的小费；helper 仅在真实确认弹框可见时点击 Yes，随后价格汇总 Tips 等于输入金额。返回文案比较因读取失败时会回退预期字符串，不能证明真实提示文字；但提示词要求的付款后提示、Yes 动作和加费成功仍由实际路径及最终 Tips 覆盖 | 无需新增 | — | 2026-07-14：复核信用卡 Paid 路径、弹框分支、Yes 与最终 Tips |
| 40 | POS-34106 | POS-34106 点单页面，礼品卡实体卡新增页面，手机号输入框，手机号输入正确，可新建卡成功 | 礼品卡 | 已自动化（当前环境直接 Failed） | POS 首页通过 `pos-ui-function-card-gift_card` 进入 Gift Card 管理页，点击真实 `Add New Card` 文本后填写 `#vpCustName`、`#vpCardNum`、`#vpPhoneNum`，手机号严格断言格式化为 `(254)429-6158`；保存时严格读取 `POST /kpos/ws/kposService` 的 `SaveGiftCardType` 请求，并校验 number、issuedTo、cellphone、`cardType=PHYISCAL`。当前环境固定返回 HTTP 404 和网络错误提示，正式用例直接 Failed，未使用 skip 或 `test.fail(...)`；若服务恢复并创建成功，finally 通过 `/api/ecard/cards/delete` 按卡号清理 | `pages/gift-card.page.ts`、`flows/gift-card.flow.ts`、`api/clients/gift-card-api.client.ts`、`test-data/order-service.ts`、`tests/py-migrate/gift-card.spec.ts` | `ORDER-PAGE-024` | 2026-07-20：TypeScript 检查通过；真实 UI 稳定复现保存接口 HTTP 404，1 failed（7.3s），等待环境修复 |
| 41 | POS-34873 | POS-34873 用户无Edit Order->Void Printed Item权限，删除hold的菜品（reduce删除），输入有权限的密码，可删除成功 系统中在staff中预置了用户1（非boss权限），以防与其他用户冲突 | 权限与 Void | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts`：[POS-34873] 应能拦截无权限员工删除 Hold 菜品并通过主管口令完成删除；关联缺陷 POS-35330 补齐真实步骤，使用 API 创建无 `VOID_KITCHEN_ITEM` 权限员工，UI 创建两菜无桌堂食订单并 Send，从保存响应按菜名取得 `orderItemId`，通过正式 Hold API 建立 `sendToKitchenRemainingDelay=-100` 前置，再从 Recall 精确编辑该单，点击真实 Reduce，断言权限提示与数字口令面板，输入主管口令并保存；关键断言：受限操作被拦截、授权后弹层关闭、Recall 详情中目标菜保留记录并标记 `VOIDED` | `api/clients/kitchen-api.client.ts`、`api/setup/kitchen.setup.ts`、`pages/order-dishes/order-dishes-navigation.ts`、`flows/order-kitchen.flow.ts`、`flows/order-permission.flow.ts`、`tests/py-migrate/order-page-recorded-regression.spec.ts` | `ORDER-PAGE-025` | 2026-07-20：TypeScript 检查通过；真实 UI 通过，1 passed（13.8s） |
| 42 | POS-35325 | POS-35325 用户无Edit Order->Void Printed Item权限，删除延迟送厨的菜品（count数量减少），输入有权限的密码，可删除成功 系统中在staff中预置了用户1（非boss权限），以防与其他用户冲突 | 权限与 Void | 已自动化（普通 Failed） | `tests/py-migrate/order-page-recorded-regression.spec.ts`：[POS-35325] 应能拦截无权限员工通过 Count 删除 Delay 菜品并通过主管口令完成删除；使用 API 创建无 `VOID_KITCHEN_ITEM` 权限员工，UI 创建两菜无桌堂食订单并 Send，从保存响应按菜名取得 `orderItemId`，通过 `/api/v1/kitchen/items/{orderItemId}/delay` 建立一小时 Delay 前置，再从 Recall 精确编辑该单并把目标菜 Count 设置为 `0`。当前产品未显示 `VOID_KITCHEN_ITEM` 授权提示，菜品数量仍为 `1`，正式用例直接失败在授权提示断言且未使用 `test.fail(...)`；修复后将继续输入主管口令、保存并在 Recall 断言目标菜为 `VOIDED` | `api/clients/kitchen-api.client.ts`、`api/setup/kitchen.setup.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`flows/order-kitchen.flow.ts`、`flows/order-permission.flow.ts`、`tests/py-migrate/order-page-recorded-regression.spec.ts` | `ORDER-PAGE-026` | 2026-07-20：真实 UI 稳定复现 Count=0 无权限提示，普通 Failed，等待产品修复 |
| 43 | POS-34895 | POS-34895 设置不自动合并相同菜，点单页面选中相同菜，1菜1行，不合并 | 送厨与合并菜 | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts`：[POS-34895] 应能按配置将连续添加的相同菜分为独立订单行；配置 API 索引确认原步骤对应两项真实设置：`COMBINE_THE_SAME_DISHES='0'` 与 `BREAK_OR_COMBIN_SAME_DISHES=true`，测试读取原值并在 finally 恢复，刷新 POS 后进入无桌堂食连续添加三次 `普通菜1`；关键断言：同名菜精确展示 3 行且每行数量原文均为 `1` | `api/setup/system-configuration.setup.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`test-data/order-service.ts`、`tests/py-migrate/order-page-recorded-regression.spec.ts` | `ORDER-PAGE-027` | 2026-07-20：TypeScript 检查通过；真实 UI 通过，1 passed（15.1s） |
| 44 | POS-34903 | POS-34903 设置相同菜合并显示(状态一致的)，已经存在不含有option的菜（已送厨），新加菜分开显示 | 送厨与合并菜 | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts`：[POS-34903] 应能仅合并状态一致的相同菜并与已送厨菜分行展示；真实配置组合确认 `COMBINE_THE_SAME_DISHES='1'` 与 `BREAK_OR_COMBIN_SAME_DISHES=false` 表示仅合并状态一致的相同菜，测试读取原值并在 finally 恢复；UI 创建一行普通菜并 Send，从 Recall 精确回开同单后连续添加两次同菜；关键断言：同名菜仅有 2 行，已送厨行数量为 `1` 且包含 `Sent in`，两次新加的未送厨同状态菜合并为数量 `2` 的另一行 | `pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`test-data/order-service.ts`、`tests/py-migrate/order-page-recorded-regression.spec.ts` | `ORDER-PAGE-028` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（20.2s） |
| 45 | POS-34910 | POS-34910 设置相同菜合并显示(包含已送厨），相同菜已送厨，编辑后继续加相同菜，加入到已送厨的一行， 保持合并展示，显示出 X In Kitchen，菜品字体红色 | 送厨与合并菜 | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts`：[POS-34910] 应能将新加同菜合并到已送厨行并显示厨房数量和红色菜名；真实配置组合确认 `COMBINE_THE_SAME_DISHES='2'` 与 `BREAK_OR_COMBIN_SAME_DISHES=false` 表示包含已送厨菜也合并，测试读取原值并在 finally 恢复；UI 创建普通菜并 Send，从 Recall 精确回开同单后再加一份同菜；关键断言：同名菜只有 1 行、总数量为 `2`、页面真实厨房数量标记为 `1 sent`、菜名计算颜色为产品红色 `rgb(130, 0, 20)`。源步骤中的 `print` 不影响标题主要校验点，按用户确认不纳入断言 | `pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`test-data/order-service.ts`、`tests/py-migrate/order-page-recorded-regression.spec.ts` | `ORDER-PAGE-029` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（19.5s） |
| 46 | POS-34842 | POS-34842 Automatically redirect after reduce items开关关闭，相连两个菜属于不同类别，当前选中菜品减到没有时，停留 原category | 数量 | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts`：[POS-34842] 关闭减菜自动跳转后应在删除跨类别菜品时停留当前类别；配置 API 索引确认真实开关为 `AUTOMATICALLY_REDIRECT_AFTER_REDUCE_ITEMS=false`，测试读取原值并在 finally 恢复；UI 从“全类型类”添加 `普通菜1`，再从 `Category 001` 添加 `superman item4`，选中后 Reduce 一次删除；关键断言：`普通菜1` 保留、`superman item4` 消失，删除前后当前类别均为 `Category 001` | `pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`test-data/order-service.ts`、`tests/py-migrate/order-page-recorded-regression.spec.ts` | `ORDER-PAGE-030` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（17.7s） |
| 47 | POS-33186 | POS-33186 点单，修改菜的数量为小于1的小数，减菜正常 | 数量 | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts`：[POS-33186] 小数数量菜品应先按一份递减再在不足一份时删除；按用户提供的原始 Zephyr 截图确认业务规则为 `1.25 → 0.25 → 删除`，配置 API 索引确认真实开关为 `ORDER_COUNT_CAN_BE_DECIMAL=true`，测试读取原值并在 finally 恢复；UI 进入 To Go 添加普通菜，通过 Count 设置 `1.25`，连续点击真实 Reduce；关键断言：第一次后原始数量为 `0.25`，第二次后同名订单行不可见 | `pages/order-dishes/order-dishes-reads.section.ts`、`test-data/order-service.ts`、`tests/py-migrate/order-page-recorded-regression.spec.ts` | `ORDER-PAGE-031` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（35.6s） |
| 48 | POS-33241 | POS-33241 点单，包含小数数量的菜，拖拽分单正常，母单和子单菜的小数数量展示正常，价格正确 | 分单与合单 | 已等价覆盖 | `tests/py-migrate/order-page-recorded-regression.spec.ts:516`；[POS-33241] 应能通过点击菜品新增子单并保持小数数量和价格正确；用户提供的真实录制确认原用例“拖拽分单”描述错误，实际路径为点击 `普通菜2` 后点击 `Add Suborder` 并确认。测试开启 `ORDER_COUNT_CAN_BE_DECIMAL`，To Go 添加 `普通菜1` 并改为 `2.55`，再添加 `普通菜2`；分单页点击后者新增子单，读取含 `普通菜1` 的子单并断言数量 `2.55`、行金额 `$22.44`、子单总数为 2；提交后从 Recall 精确打开父订单及对应子单，再次断言菜名与数量 `2.55` | `pages/split-order.page.ts`、`flows/split-order.flow.ts`、`flows/recall.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order-page-recorded-regression.spec.ts` | `ORDER-PAGE-032` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（38.7s） |
| 49 | POS-33244 | POS-33244 点单，包含小数数量的菜，合单，订单菜的数量和价格正确 | 分单与合单 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:172；[POS-33244] 应能合并含小数数量的两笔 To Go 订单并保持数量和小计守恒；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesFlow.addRegularDish` → `OrderDishesPage.changeOrderedDishQuantity/saveOrderWithReference` → `RecallFlow.openRecallFromHome/combineOrders` → `RecallPage.openOrderDetails/readOrderDetailsSnapshot`；关键断言：按两笔精确订单号合单后数量为 `2.55`、`1`，Subtotal 整数分等于两单合并前之和 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 50 | POS-33600 | POS-33600 点单，选择指定价格的菜，修改菜的数量为指定小数，保存订单成功 | 数量 | 已等价覆盖 | `tests/py-migrate/order-page-regression.spec.ts:300`；[POS-33600] 应能保存改价为 6.50 且数量为 2.55 的菜品和两份追加菜；测试显式开启并 finally 恢复 `ORDER_COUNT_CAN_BE_DECIMAL`，刷新 POS 后从 To Go 添加普通菜1、改价为 `$6.50`、数量改为 `2.55`，再添加两份普通菜2；真实 UI 确认半分按订单行舍入为 `$16.58`。关键断言：点单页目标行金额为 1658 分、普通菜2总数量为 2；按运行期订单号在 Recall 精确回查后，目标数量仍为 `2.55`、行金额仍为 1658 分、追加总数量仍为 2，且保存前后 Subtotal 整数分一致 | `test-data/order-service.ts`、`tests/py-migrate/order-page-regression.spec.ts` | `ORDER-PAGE-033` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（41.4s） |
| 51 | POS-33600 | POS-33600 点单，选择指定价格的菜，修改菜的数量为指定小数，保存订单成功 | 数量 | 已等价覆盖 | tests/py-migrate/order-page-regression.spec.ts:249；[POS-33600] 应能保存改价为 6.50 且数量为 1.5 的菜品并保持小计一致；Page/Flow 链：`TakeoutFlow.startToGoOrder` → `OrderDishesFlow.addRegularDish` → `OrderDishesPage.changeOrderedDishPrice/changeOrderedDishQuantity/readPriceSummary/saveOrderWithReference` → `EmployeeLoginFlow.enterEmployeeContext` → `RecallFlow.openRecallFromHome` → `RecallPage.openOrderDetails/readOrderDetailsSnapshot`；关键断言：6.50 × 1.5 行金额为 975 分、按精确单号回查数量为 `1.5` 且保存前后 Subtotal 整数分一致 | 无需新增 | — | 2026-07-14：最终真实 UI 12/12 通过 |
| 52 | POS-35129 | POS-35129 点单，选择指定价格的菜，修改菜的数量为指定小数，保存订单成功 | 数量 | 已等价覆盖 | `tests/py-migrate/order-page-regression.spec.ts:385`；[POS-35129] 应能保存数量为 2.55 的菜品并在 Recall 保持数量和价格；按用户明确确认将 Recall 持久化作为主要校验点。测试显式开启并 finally 恢复 `ORDER_COUNT_CAN_BE_DECIMAL`，刷新 POS 后从 To Go 添加普通菜1并将数量改为 `2.55`；保存前断言行金额为 `$22.44`，保存后按运行期订单号精确打开 Recall，断言数量仍为 `2.55`、行金额仍为 `$22.44`，且保存前后 Subtotal 整数分一致。Jira 当前标题已变为“关闭小数开关后不能输入小数”，与源提示词冲突；本覆盖范围遵循用户确认的源提示词，不擅自改写 | `test-data/order-service.ts`、`tests/py-migrate/order-page-regression.spec.ts` | `ORDER-PAGE-034` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（39.4s） |
| 53 | POS-35660 | POS-35660 开启相同菜合并展示开关，点单，修改菜的数量为小数，给菜添加调味正常 | Modify | 已等价覆盖 | `tests/py-migrate/order-page-regression.spec.ts:453`；[POS-35660] 开启相同菜合并后应能为小数数量菜品添加两份全局调味；测试显式设置并 finally 恢复 `COMBINE_THE_SAME_DISHES='1'`、`BREAK_OR_COMBIN_SAME_DISHES=false`、`ORDER_COUNT_CAN_BE_DECIMAL=true`，刷新 POS 后创建 To Go 订单，将普通菜1改价为 `$7.95`、数量改为 `2.3`，通过真实 Modify/加柴/Count 控件把调味数量设为 2。关键断言：点单页行金额按半分舍入为 `$18.29`、数量 `2.3`、加柴数量 2；保存后 Recall 数量、调味结构化 quantity=2、行金额和 Subtotal 均与保存前一致 | `pages/recall/recall.types.ts`、`pages/recall/recall-order-details.dialog.ts`、`test-data/order-service.ts`、`tests/py-migrate/order-page-regression.spec.ts` | `ORDER-PAGE-035` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（41.3s） |
| 54 | POS-22640 | POS-22640 自定义类型-POS点单点击自定义类型 Delivery，点单，打单成功 | 自定义类型与打印 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts`：[POS-22640] 自定义 Delivery 应能保存订单并打单成功。新增可恢复的自定义订单类型 setup，测试前读取并注册 `CUSTOM_ORDER_TYPE1` 与首页布局原值，映射为 `DELIVERY`、显示首页稳定入口 `pos-ui-function-card-custom_order_type1`，结束后由资源注册表自动恢复。真实 UI 确认点击该入口后需再次输入员工口令，随后进入标准 Delivery 信息页；填写姓名、手机号、地址并添加普通菜1/普通菜2。保存请求严格断言 `order.type=CUSTOM_ORDER_TYPE1`，Recall 按运行期订单号回查客户与菜品；点击 Print 后严格等待真实 `POST /kpos/api/print/kitchen/ticket`，同时校验 HTTP 200 和业务响应 `code=0`，作为打单成功信号，不再猜测本地打印目录 | `api/clients/order-type-api.client.ts`、`api/clients/layout-config-api.client.ts`、`api/setup/custom-order-type.setup.ts`、`pages/home.page.ts`、`pages/recall/recall-order-details.dialog.ts`、`flows/takeout.flow.ts`、`flows/recall.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-036` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（24.1s）；配置结束后回读确认已恢复原值 |
| 55 | POS-36286 | POS-36286 POS首页delivery下单，添加用户信息进入订单页面，点击退出直接退出点单页 | 客户信息 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:442`；[POS-36286] Delivery 填写客户信息进入点单页后应能无提示直接退出。测试从 POS 首页进入固定 Delivery，填写客户姓名、电话和有效地址进入点单页，不添加菜品；通过实测宿主页唯一 `Back` 按钮退出，断言退出确认按钮始终隐藏，并以 POS 首页固定功能卡作为返回完成信号。点单页 Back 已收敛为真实 DOM 的单一语义定位，不再使用 iframe/宿主页/ID 候选链 | `pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-navigation.ts`、`pages/order-dishes.page.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-037` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（9.8s） |
| 56 | POS-36255 | POS-36255 菜名名称与number（如均为AA）相同时，点单页面搜索只能搜到一个AA | 菜单与搜索 | 已自动化（当前失败） | `tests/py-migrate/order.service.spec.ts:469`；[POS-36255] 菜品名称与编号相同时搜索结果应只展示一次。测试通过后台 API 创建并自动清理 `name='AA'`、`itemNumber='AA'`、`price=10` 的隔离临时菜，显式开启并恢复 `SEARCH_MENU`，刷新 POS 后进入 To Go 搜索 `AA`；页面按真实语义契约读取全部 `button[name='AA.AA']` 并直接断言数量为 1，不使用 skip 或预期失败 | `test-data/api/menu-api-data.ts`、`test-data/order-service.ts`、`pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`pages/order-dishes.page.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-038` | 2026-07-20：TypeScript 检查通过；真实 UI 结果 Expected 1 / Received 2，正式用例直接失败，等待 POS-36255 产品缺陷修复 |
| 57 | POS-37804 | POS-37804 无note权限用户，套餐子菜加note出权限提示 | 权限与 Void | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts`：[POS-37804] 无 Note 权限员工为套餐子菜添加备注时应要求授权。Atlassian 关联缺陷 POS-36081 明确原始流程和预期；接口确认 NOTE 权限为 ID 16。测试复制 Server 角色并移除 NOTE，创建一次性员工且通过 `/api/admin/privilege/check` 回读确认无权限；以该员工创建无桌堂食套餐并用稳定子菜契约选中普通菜1，点击 `action-rail-button-addnote` 后严格断言真实权限提示和口令表单，输入主管口令 11 后填写“备注信息”。保存请求结构化断言 Note 位于目标子菜 `comboOrderDetails.comboSections[].orderItems[].options` 且 `optionType=NOTE`，套餐主菜 `displayText` 仍为空；Recall 精确订单回查备注持久化。资源注册表按先员工后角色的顺序自动清理，实测结束后无残留 | `api/setup/staff.setup.ts`、`test-data/api/staff-api-data.ts`、`pages/order-dishes/order-dishes-note.section.ts`、`pages/order-dishes/order-dishes-locators.ts`、`flows/order-permission.flow.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-039` | 2026-07-20：Atlassian 原用例关联 POS-36081；TypeScript 检查通过；真实 UI 正式用例通过，1 passed（14.2s）；一次性员工与角色清理完成 |
| 58 | POS-42060 | 必选类功能优化，未满足条件时无法提交，且自动跳转 | 菜单配置 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:629`；[POS-42060] 未满足必选类别时应阻止保存并自动跳转到该类别；API 动态创建 `requireCategory=true` 的必选类别及关联菜品，刷新后先只点普通类别菜并保存，断言仍停留点单页且选中类别自动变为未满足的必选类别；补点该类别菜品后保存接口成功并返回订单号；资源注册表按 ID 清理临时菜品和类别 | `test-data/api/menu-api-data.ts`、`test-data/order-service.ts:107`、`pages/order-dishes/order-dishes-menu.section.ts:209`、`pages/order-dishes/order-dishes-navigation.ts:151`、`tests/py-migrate/order.service.spec.ts:629` | — | 2026-07-20：Atlassian 原 Jira description 为空；真实 UI 1/1 通过（37.7 秒），TypeScript 编译通过 |
| 59 | POS-42958 | category未勾选“限制折扣”导致该category下所有菜品可以参与整单按比例加收 | 菜单配置 | 已自动化（当前失败） | `tests/py-migrate/order.service.spec.ts:690`；[POS-42958] 未启用整单折扣适用性的类别菜品不应参与整单比例加收。Atlassian 原 Jira 标题明确为“所有菜品不可以参与”，与提示词源遗漏“不”字相反，故以 Jira 为准。API 动态创建 `applicableToOrderDiscount=false` 的类别并回读确认配置持久化，加入该类别两道菜（4.00、6.00）及可参与对照菜（8.80），应用 10% 整单加收后直接断言 Charge 应仅为 0.88；用例不使用 skip 或预期失败 | `test-data/order-service.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`flows/order-dishes.flow.ts:434`、`tests/py-migrate/order.service.spec.ts:690` | `ORDER-PAGE-041` | 2026-07-20：TypeScript 检查通过；真实 UI 实际 Charge=1.88（全部 18.80 均被计入），Expected 0.88 / Received 1.88，正式用例直接失败，等待 POS-42958 产品缺陷修复 |
| 60 | POS-42097 | 新UI点单页面，category正常展示配置的POS NAME | 菜单配置 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:570`；[POS-42097] 点单页面类别应展示配置的 POS NAME；API 动态创建后台名称 A、POS NAME B 不同的临时类别及关联菜品，刷新 POS 后在真实菜单组读取全部类别卡，断言出现 B 且不出现 A；选择 B 后断言选中类别名严格为 B，并确认类别下存在关联菜品；资源注册表按 ID 清理临时菜品和类别 | `test-data/api/menu-api-data.ts`、`test-data/order-service.ts:78`、`pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-menu.section.ts:193`、`tests/py-migrate/order.service.spec.ts:570` | — | 2026-07-20：真实 UI 1/1 通过（37.7 秒），TypeScript 编译通过 |
| 61 | POS-42061 | 套餐的子菜支持改价 | 套餐 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:949`；快速添加 `套餐1` 并选择 `option1` 后，使用子菜稳定契约 `dish-item-subitem-combo-698` 精确选中 `普通菜1`，确认 Price 动作可用并输入 5.50；页面层分别读取目标子菜价格和套餐父行总价，断言子菜从 8.80 变为 5.50、父行总价严格按价差减少 3.30，随后保存订单 | `test-data/order-service.ts`、`pages/order-dishes/order-dishes-locators.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`pages/order-dishes.page.ts`、`tests/py-migrate/order.service.spec.ts:949` | `ORDER-PAGE-043` | 2026-07-20：真实 UI 1/1 通过（11.0 秒）；TypeScript 编译通过 |
| 62 | POS-36254 | 按菜分单后子单打折，打折界面整单金额检查 | 分单与折扣 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1154`；[POS-36254] 按菜分单后子单折扣界面应显示当前子单整单金额。按用户提供的真实动作点击菜品并通过 Add Suborder 建立第二个子单，不采用源描述中错误的拖拽；提交后从 Recall 精确打开新增子单，先断言该子单仅包含被移动的普通菜2并读取其 Subtotal，再点击详情中的 Discount。页面对象以真实 `pos-ui-segmented-option-whole` 契约确认 Whole Order 作用域，并读取弹窗内子单号与 `_wholeOrderSubtotalValue_` 数值；最终严格断言弹窗子单号等于当前子单号、Whole Order Subtotal 等于子单详情 Subtotal，而非母单金额或含税/加收后的 Total | `pages/recall/recall.types.ts`、`pages/recall/recall-order-details.dialog.ts`、`pages/recall.page.ts`、`test-data/order-service.ts`、`tests/py-migrate/order.service.spec.ts:1154` | `ORDER-PAGE-044` | 2026-07-20：TypeScript 检查通过；真实 UI 正式用例通过，1 passed（15.6s） |
| 63 | POS-22657 | 自定义类型-报表显示Report-Overview-预览显示自定义类型的报表数据-数据正确 | 报表 | 需要录制 | 最小录制路径：从 POS 首页记录 Cloud Report 原配置并启用，按源顺序打开/读取 Report Overview 的 `cloud report amount` 基线，单独读取启用后出现的“打印提示信息”及其真实位置/文案，再回首页刷新 POS 使配置生效；随后从首页创建自定义 Delivery 订单（name=`pos-test`、phone=`01234567890`）、加菜并保存，从首页再次打开 Cloud Report，切换真实 report frame/window，在 Report Overview 选择目标自定义订单类型并读取最终数据；最后恢复原配置并再次刷新 POS。前置数据：目标自定义订单类型、云报表账号/服务和可恢复配置；`ReportPage` 当前为空，`HomePage.enterReport` 只点击入口，自定义类型入口也缺失。“打印提示信息”疑似源步骤误写，但在产品确认前必须保留为独立中间断言，不能替代或合并最终 Overview 数据断言。当前缺失的准确 UI 动作是“启用后读取 amount/打印提示、刷新，再进入 frame 选择类型并读最终报表”。保留证据：Cloud Report 配置 ID/旧值/新值及启用/恢复请求，两次 POS 刷新信号，Overview amount 单元、打印提示容器/文案/触发时机、Report 入口、frame/window、Overview tab、订单类型筛选和数据单元稳定 DOM，订单保存 payload 的 customOrderTypeId/amount 与报表查询响应的类型 ID、时间范围、数量/金额。最终断言：启用阶段 amount 可读且打印提示符合录制结果（若确认源误写则记录产品结论后调整）；最终 Report Overview 中目标类型数量/金额相对基线按新订单精确增加并与保存订单一致；finally 恢复配置且刷新生效。职责：system configuration setup 负责配置恢复，`pages/home.page.ts` 负责两次刷新和首页提示（若提示实属其他页则按录制归属），`pages/report.page.ts` 负责 amount/frame/筛选/读数，`flows/report.flow.ts` 负责编排启用、基线、提示、刷新、增量与恢复，`flows/takeout.flow.ts` 负责自定义 Delivery | `api/setup/system-configuration.setup.ts`、`pages/home.page.ts`、`pages/report.page.ts`、`flows/report.flow.ts`、`flows/takeout.flow.ts`、`tests/py-migrate/order.service.spec.ts` | `ORDER-PAGE-045` | 2026-07-14：恢复 Cloud Report amount、独立打印提示、启用后刷新、最终 Overview 与恢复后刷新全部源步骤 |
| 64 | POS-43827 | 系统语言为中文，点单搜索框输入菜的首字母返回对应的菜 | 语言与搜索 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:512`；[POS-43827] 中文界面按菜品拼音首字母搜索应返回对应菜品；`HomePage.readCurrentLanguage/switchLanguage` 通过首页堂食卡稳定文案读取并切换语言；中文点单页用真实精确语义按钮“搜索菜单”打开搜索，输入 `ptc` 后按稳定 `menu-item-card-dsh0_0_698` 定位“普通菜1”，确认进入订单行并保存；finally 恢复英文及 `SEARCH_MENU` 原配置 | `pages/home.page.ts:133`、`pages/order-dishes/order-dishes-locators.ts:239`、`pages/order-dishes/order-dishes-menu.section.ts:125`、`test-data/order-service.ts:71`、`tests/py-migrate/order.service.spec.ts:512` | — | 2026-07-20：真实 UI 1/1 通过（37.7 秒），TypeScript 编译通过 |
| 65 | POS-43956 | 套餐设置display all one time，子菜不可重复选，规则设置max，点单保存combo后，可正常编辑，修改子菜 | 套餐 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1069`；打开现有规则套餐 `套餐1` 后同时断言候选 `普通菜1/普通菜2` 可见，选择 max 结果后订单仅保存一份 `普通菜2`；保存订单号并从 Recall 精确 Edit，通过稳定子菜契约 `dish-item-subitem-combo-699` 选中子菜、打开 Modify、添加 `加柴` 并再次保存。最终 Recall 严格确认目标子菜仅一份，主菜 `option1` 与子菜调味 `加柴` 均持久化 | `test-data/order-service.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`pages/order-dishes/order-dishes-modifier.section.ts`、`pages/order-dishes.page.ts`、`flows/recall.flow.ts`、`tests/py-migrate/order.service.spec.ts:1069` | `ORDER-PAGE-047` | 2026-07-20：按源步骤“modify combo sub item”实现对子菜调味修改；真实 UI 1/1 通过（14.7 秒）；TypeScript 编译通过 |
| 66 | POS-43823 | combo子菜没有option，选择子菜返回主菜可正常选择option | 套餐 | 已等价覆盖 | `tests/py-migrate/order.service.spec.ts:1005`；打开 `套餐1` 后精确选择无 option 子菜 `普通菜2`（`3-699-1`），随即确认主菜 option `option1` 可见并可选择；再添加带自身 option `A/a1` 的普通菜1作为归属对照。订单行断言套餐 additions 严格为 `普通菜2, option1` 且子菜无 subAdditions，普通菜 additions 严格为 `A, a1` 且不含主菜 option，随后保存订单 | `test-data/order-service.ts`、`pages/order-dishes/order-dishes-menu.section.ts`、`pages/order-dishes/order-dishes-reads.section.ts`、`pages/order-dishes.page.ts`、`tests/py-migrate/order.service.spec.ts:1005` | `ORDER-PAGE-048` | 2026-07-20：Atlassian Jira description 为空；真实 UI 1/1 通过（10.4 秒）；TypeScript 编译通过 |
