# Playwright Page / Flow 能力补全需求模板

请基于当前仓库补充或完善 Playwright 自动化中的页面能力与业务流程能力，并严格遵守仓库中的 `AGENTS.md`、现有目录结构、命名规范、分层约束与代码风格。

## 项目信息

- 项目地址：`【仓库地址】`
- 框架说明：`【项目框架说明文档地址】`
- 规则与约束：`【AGENTS.md 地址】`
- 页面源码目录：`【本地源码路径，可选】`

## 任务目标

- 任务：`【填写要补充的 page / flow 能力】`
- 只做：`【明确本次只做什么】`
- 不做：`【明确本次不做什么】`

目标是补齐当前仓库中缺失或不足的 `pages/...` 与 `flows/...` 能力，使其能够稳定支撑后续测试或业务调用。

## 执行前要求

在开始编码前，请先完成以下事项：

1. 阅读并遵守 `AGENTS.md`
2. 优先复用当前仓库已有的：
   - pages
   - flows
   - fixtures
   - helpers / utils
   - 测试数据组织方式
3. 检查仓库中是否已经存在同目标或高相似能力
4. 若已有同目标或高相似能力，优先在原文件上增量补充，不重复新建同类对象
5. 如现有能力不足，可在符合分层原则前提下补充代码
6. 不要破坏现有风格，不要引入与仓库规范冲突的实现方式

## 页面进入信号

- 当前页面地址或 hash：`【页面地址 / hash / 路由标识】`
- 页面加载完成信号：`【顶部按钮 / 标题 / 关键区域 / iframe 等信号】`
- 列表或主体内容信号：`【列表卡片 / 表格 / 表单主体 / 详情区域等信号】`
- 若页面运行在 iframe 中：`【明确 iframe 信号】`

## 已有能力检查要求

请先检查仓库中是否已有以下能力：

- `【已有 page 能力1】`
- `【已有 page 能力2】`
- `【已有 flow 能力1】`
- `【已有测试或契约验证】`

如果已有能力不足，请仅补充本次场景所需的最小必要能力。

## 需要实现的 Page 能力

请将以下能力放在 `pages/...` 中，且只表达页面结构、页面动作与页面读取：

1. `【页面动作1】`
2. `【页面读取2】`
3. `【页面读取3】`
4. `【页面读取4】`
5. `【页面读取5】`
6. `【页面读取6】`
7. `【页面读取7】`

补充要求：

- page 不要写业务选择策略
- 所有稳定 selector 必须集中定义在 page object 中
- 优先使用 `data-testid`
- 没有稳定 `data-testid` 时，再退回 `getByRole` / `getByLabel` / `getByText`
- 不要把强耦合、只存在于父页面单步流程中的瞬时弹窗拆成独立 page object
- 所有 page 公共方法都应使用中文 `@step(...)`

## 需要实现的 Flow 能力

请将以下能力放在 `flows/...` 中，且只表达业务意图与多步编排：

1. `【flow 能力1】`
2. `【flow 能力2】`

补充要求：

- flow 表达业务意图
- flow 可以组合多个 page 方法、处理顺序、兜底、异常恢复
- flow 不要重写 page locator
- 若读取详情后会污染当前页面状态，flow 必须负责恢复
- 若存在可选分支或回退逻辑，应放在 flow 中，而不是 page 中

## 成功标准

- `【成功标准1】`
- `【成功标准2】`
- `【成功标准3】`

## 实现约束

- 使用 `Playwright Test`
- `describe` / `test` / `test.step` 使用中文命名
- 页面动作放在 `pages`
- 业务编排放在 `flows`
- 测试文件只保留测试意图与断言，不堆砌底层操作细节
- 优先使用 `data-testid`
- 禁止使用 `waitForTimeout`
- 禁止通过直跳业务内页 URL 绕过正常入口流程，除非本次任务明确是纯页面契约解析且用户已指定页面上下文
- 优先复用已有 page object；如需补充，请保持 pages / flows 职责清晰
- 选择器要稳定，避免过度依赖文案、层级、nth()
- 如定位不稳定，应优先补充 page object / flow，而不是把复杂 locator 直接堆在 spec 中
- 如页面字段可能为空，返回结构中应明确表达可空语义，而不是抛出无意义异常
- 如读取结果为动态数据，请返回结构化快照对象，便于测试做对比校验

## 验证要求

请至少补充一种真实验证方式，优先级如下：

1. `【已有 smoke / e2e / regression 用例复用验证】`
2. `【最小必要的契约测试或 e2e 测试】`
3. `【若无法自动化验证时的说明】`

如果读取的是详情弹窗、订单面板或复杂 DOM 区域，建议增加最小契约测试，锁定解析结果。

## 代码产出要求

请直接修改或新增以下内容（按实际需要）：

- `pages/...`
- `flows/...`
- `tests/...`

并确保：

1. 新增代码可运行
2. 命名符合仓库风格
3. 不引入无关重构
4. 仅实现本次场景所需的最小必要改动

## 输出要求

完成后请输出：

1. 本次新增或修改了哪些文件
2. 核心实现说明
3. 执行命令
4. 实际验证结果
5. 如未能完成，请明确说明卡点与原因，不要伪造完成状态

补充要求：

- 必须运行相关 Playwright 用例，并基于真实执行结果汇报
- 不允许仅根据代码推断“理论上应当通过”
- 如果只完成了代码修改但未完成验证，必须明确说明未验证部分与原因
